package com.example.pruebastecnicas

import kotlin.random.Random

// Demostrar el proceso de recorrido (iteracion) de una lista con un clclo "for".

/*fun main() {
    val partesComputador = listOf<String>("Monitor", "CPU", "RAM", "SSD", "Board")

    for (parte in partesComputador){
        println("La parte actual es : $parte")
    }
}

//Realizar una demostracion basica del uso del ciclo while.

fun moverFigura(){
    println("La figura se ha movido.")
}


fun main() {
    var contadorMovimiento = 0

    while (contadorMovimiento < 10){
        moverFigura()

        ++contadorMovimiento       //"++" operador de preincrementar //incremento unitario
                                   // por cada iteracion la var contadorMovimiento  aumentara en una unidad
    }
}

// Ciclo while indeterminado

fun main() {
    while (true){
        var aleatorio = Random(System.nanoTime()).nextInt(1, 10)
         println("El valor aleatorio generado es: $aleatorio" )

        if (aleatorio == 5) {
            break
        }
    }
}


// Uso basico de "do while"
fun moverFigura(){
    println("La figura se ha movido")

}
fun main() {

    var contadorMovimiento = 0

    do {
        moverFigura()
        ++contadorMovimiento
    }while (contadorMovimiento < 10)
}



// Ciclo "do while indeterminado"


fun main() {
    do {
        var aleatorio = Random(System.nanoTime()).nextInt(1,10)

        println("El valor aleatorio generado es : $aleatorio")

        println()

        if (aleatorio == 5){
            break
        }
    }while (true)
}*/
